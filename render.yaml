services:
  - type: web
    name: enhanced-experience-platform
    env: node
    region: oregon
    plan: free
    buildCommand: pnpm install --frozen-lockfile
    startCommand: node dist/server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production

